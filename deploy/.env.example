# JWT secret for auth tokens (random string, tokens won't persist across restarts without this)
JWT_SECRET=change-me

# SMTP config for sending emails (optional — emails log to console if not set)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=noreply@yourdomain.com

# Public URL of your app (used in email links)
APP_URL=https://yourdomain.com
