# CustomChess Environment Configuration

# WebSocket server URL (optional - defaults to same host)
# For separate backend server, set this to your server's WebSocket endpoint
# VITE_WS_URL=wss://your-backend-server.com/ws
